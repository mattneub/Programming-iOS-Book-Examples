
import UIKit

class ViewController : UIViewController {
    
    // the content view in the nib is sized internally
    // by views with width and height constraints
    
    // note that in iOS 11 I have to set the content insets pop-up to Always
    // otherwise we don't scroll initially 
    
    // also we really didn't need a content view to accomplish this, it's just convenient
    
}
